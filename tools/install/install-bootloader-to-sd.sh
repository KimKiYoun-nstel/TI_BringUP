#!/usr/bin/env bash
set -euo pipefail

echo "[TODO] Install tiboot3.bin, tispl.bin, and u-boot.img to the SD boot partition."
echo "[INFO] Keep this script board/media-specific before enabling writes."
