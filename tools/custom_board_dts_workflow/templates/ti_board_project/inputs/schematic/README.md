# Schematic Inputs

이 디렉터리에는 board 회로도 PDF를 둔다.

반복 작업에서는 `hardware_db/` 아래에 PDF 기반 backdata를 함께 두는 것을 권장한다.

권장 규칙:

- 원본 PDF는 수정하지 않는다.
- 파일명에 board명과 revision을 드러낸다.
- `docs/board_dts_decisions.yaml`의 `source_documents`와 경로를 맞춘다.
