# Netlist Inputs

이 디렉터리에는 board 회로도에서 생성한 `.NET` 파일을 둔다.

권장 규칙:

- 실제 분석에 쓴 netlist 한 벌을 명확히 남긴다.
- 파일명에 board명과 revision을 드러낸다.
- `docs/board_dts_decisions.yaml`의 `source_documents`와 같은 파일명을 사용한다.
