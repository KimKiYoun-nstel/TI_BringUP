# Linux BSP Area

This directory tracks AM64x Linux kernel patches, config fragments, DTS candidates, and notes exported from `workspace/ti-linux-kernel-sdk12`.

Do not copy the full Linux source tree into this repo.
