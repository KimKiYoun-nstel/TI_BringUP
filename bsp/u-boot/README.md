# U-Boot BSP Area

This directory tracks AM64x U-Boot patches, config fragments, DTS candidates, and notes exported from `workspace/ti-u-boot-sdk12`.

Do not copy the full U-Boot source tree into this repo.
